{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "apps/site/Dockerfile"
  },
  "deploy": {
    "watchPatterns": ["apps/site/**", "pnpm-lock.yaml", "pnpm-workspace.yaml"]
  }
}
