// Phase 4b IA consolidation panel — alias of the existing UserManagementPage
// so the `/admin/users?tab=usuarios` host can mount it as a tab panel.

export { UserManagementPage as UsersPanel } from "@/features/users/pages/UserManagementPage";
