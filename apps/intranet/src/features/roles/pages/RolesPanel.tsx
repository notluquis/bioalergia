// Phase 4b IA consolidation panel — re-export of the existing roles settings
// page so the `/admin/users?tab=roles` host can mount it as a tab panel.

export { RolesSettingsPage as RolesPanel } from "@/pages/settings/RolesSettingsPage";
