{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "apps/intranet/Dockerfile"
  },
  "deploy": {
    "watchPatterns": ["apps/intranet/**", "packages/db/**", "pnpm-lock.yaml", "pnpm-workspace.yaml"]
  }
}
