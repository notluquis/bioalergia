{
  "name": "finanzas-monorepo",
  "private": true,
  "engines": {
    "node": ">=25.0.0",
    "pnpm": ">=10.27.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "argon2",
      "sharp",
      "husky",
      "@zenstackhq/runtime",
      "protobufjs",
      "lmdb",
      "@swc/core"
    ]
  },
  "packageManager": "pnpm@10.27.0",
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
